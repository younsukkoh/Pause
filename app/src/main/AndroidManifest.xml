<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="pause.sip.younsukkoh.pause">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="com.google.android.providers.gsf.permission.READ_GSERVICES"/>
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/logo_brown"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppThemeActionBar">

        <meta-data android:name="com.google.android.geo.API_KEY" android:value="@string/google_maps_key" />

        <activity android:name=".MainActivity"/>

        <activity android:name=".login.CreateAccountActivity01" android:theme="@style/AppThemeNoActionBar"/>

        <activity android:name=".login.CreateAccountActivity02" android:theme="@style/AppThemeNoActionBar"/>

        <activity android:name=".memory.MemoryActivity"/>

        <activity android:name=".our_room.OurRoomActivity" />

        <activity android:name=".image.ImageActivity" android:theme="@style/AppThemeNoActionBar"/>

        <activity android:name=".image.ImagePagerActivity" android:theme="@style/AppThemeNoActionBar"/>

        <activity android:name=".login.LoginActivity" android:theme="@style/AppThemeNoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>